# 할일

작성일: 2026-03-27

## 1. [완료] `주문 안내` 문의 흐름과 견적 폼 요구사항 정리

- 처리 결과
  `주문 안내` 선택 시 `수량`을 선택사항으로 바꾸고, 폼 라벨과 안내 문구도 해당 흐름에 맞게 조정했습니다.
- 관련 파일
  `product-27-faq.html`
  `quote.html`
  `assets/site.js`

## 2. [완료] 사이트맵에 공개 중인 상담 페이지 반영

- 처리 결과
  `product-05-item-7.html`, `product-24-pvc.html`를 `sitemap.xml`에 추가해 공개 링크와 사이트맵을 맞췄습니다.
- 관련 파일
  `products.html`
  `product-05-item-7.html`
  `product-24-pvc.html`
  `sitemap.xml`

## 3. [완료] 홈페이지 canonical / `og:url` URL 기준 통일

- 처리 결과
  `index.html`의 canonical 과 `og:url`을 루트 `/` 기준으로 맞춰 `sitemap.xml`과 통일했습니다.
- 관련 파일
  `index.html`
  `sitemap.xml`

## 4. [완료] `맞춤 제작 상담` / `PVC 상담` 페이지 템플릿 정리

- 처리 결과
  두 페이지 모두 문서형 안내 페이지로 전환하고, 상담 범위 요약과 문의 CTA 중심으로 단순화했습니다.
- 관련 파일
  `product-05-item-7.html`
  `product-24-pvc.html`
  `product-27-faq.html`

## 5. [완료] 홈페이지 `주요 취급 품목` 카드의 탐색 기대와 실제 동작 일치

- 처리 결과
  카드 4개 전체를 클릭 가능하게 바꾸고, 각 카드가 `products.html`의 해당 섹션 앵커로 바로 이동하도록 정리했습니다.
- 관련 파일
  `index.html`
  `assets/style.css`

## 6. [완료] 리디렉트 페이지 운영 부채 정리

- 처리 결과
  배포 전 기준으로 사용하지 않는 리디렉트 HTML 7개를 삭제하고, 내부 복구 스크립트와 점검 문서의 관련 흔적도 함께 정리했습니다.
- 관련 파일
  `tools/generate_recovery.ps1`
  `workbench/content-audit.md`

## 7. [완료] 홈페이지 첫 화면 진입 분기 명확화

- 처리 결과
  히어로는 짧게 정리하고, 바로 아래에 `기성품 견적`, `맞춤 상담`, `제작·납기 안내` 3갈래 선택 섹션을 독립 배치해 첫 화면에서 진입 경로가 먼저 보이도록 정리했습니다.
- 관련 파일
  `index.html`
  `assets/style.css`

## 8. [완료] `quote.html`을 제품 견적 모드와 일반 문의 모드로 재정리

- 반영 결과
  폼 상단에 `제품 견적` / `상담·일반 문의` 선택을 추가하고, 일반 문의에서는 `골 / 재질`, `인쇄 여부`, `희망 수령 방식`을 숨기고 `수량`을 선택값으로 낮췄습니다.
- 개선 내용
  `문의 주제`, `문의 내용`, `희망 일정(선택)`, `추가 참고 사항`처럼 일반 문의 기준 문구로 바뀌며, 연락처 입력도 `tel` 타입으로 정리했습니다.
- 연동 범위
  `주문 안내`, `맞춤 제작 상담`, `PVC 상담` 유입 시에는 일반 문의 모드가 자동으로 적용되도록 프리필 흐름까지 맞췄습니다.
- 관련 파일
  `quote.html`
  `assets/site.js`

## 9. [완료] 홈페이지 반복 정보 축소와 핵심 메시지 집중

- 반영 결과
  히어로 하단의 전화번호 재반복을 제거하고, 규격 미정 상태에서도 상담부터 진행할 수 있다는 안내만 남겼습니다.
- 개선 내용
  거래 요약 카드에서 `배송 방식`, `세금계산서`를 빼고 `배송비`, `결제 안내`, `주문 안내` 3개만 유지해 새 정보 중심으로 정리했습니다.
- 기대 효과
  홈 첫 화면과 중간 구간에서 운영 정보가 덜 중첩돼, 빠른 선택과 핵심 안내가 더 먼저 보이도록 정리했습니다.
- 관련 파일
  `index.html`
  `assets/style.css`

## 10. [완료] `products.html`에서 정보형 페이지와 상품형 카드 문법 분리

- 반영 결과
  `맞춤 제작 상담`, `PVC 상담`, `제작·납기 안내` 카드에 안내형 변형을 적용해 상품 카드와 다른 구조로 보이게 정리했습니다.
- 개선 내용
  `최소수량 / 리드타임 / 단가` 대신 `상담 대상`, `준비 정보`, `진행 방식`, `확인 내용` 같은 안내형 메타로 바꾸고, `안내 보기`를 메인 버튼으로 올렸습니다.
- 연동 범위
  `문의하기` 버튼은 일반 문의 모드가 먼저 열리도록 `quote.html`에 문의 성격과 품목 정보를 함께 넘기도록 맞췄습니다.
- 관련 파일
  `products.html`
  `assets/style.css`

## 11. [완료] 홈페이지 대표 상품 구성 재조정

- 반영 결과
  홈 대표 구성을 `택배박스 / D형박스 / 골판지(양면) / 골판지(편면)` 4축으로 다시 맞춰 실제 취급 범위를 더 고르게 드러내도록 정리했습니다.
- 개선 내용
  `D형박스`는 상담형 카드로 바꾸고, `골판지(편면)`은 `ITEM-25`를 대표 항목으로 올려 홈 카테고리와 대표 구간의 구성이 일치하도록 맞췄습니다.
- 기대 효과
  홈 중간에서 `D형박스`와 `편면 골판지`의 존재감이 빠지지 않아, 사용자가 주요 품목군을 더 균형 있게 인식할 수 있습니다.
- 관련 파일
  `index.html`
  `assets/style.css`

## 12. [대기] 홈페이지 거래 요약 5카드 구조 정리

- 현재 상태
  `배송 방식`, `배송비`, `결제 안내`, `세금계산서`는 정보 카드이고 `주문 안내`만 버튼이 있는 행동 카드입니다.
- 문제
  한 묶음 안에서 카드 역할이 섞여 시각적 리듬이 어색합니다.
- 영향
  `주문 안내` 카드가 나중에 덧붙은 요소처럼 보일 수 있습니다.
- 권장 방향
  전부 정보 카드로 맞추거나, 행동 카드만 따로 분리하는 방식 검토.
- 관련 파일
  `index.html`
  `assets/style.css`

## 13. [완료] 영어 섹션 라벨을 한국어 중심으로 통일

- 반영 결과
  홈, 상품안내, 문의 페이지에 남아 있던 영어 `section-label`을 한국어 라벨로 정리했습니다.
- 개선 내용
  `빠른 선택`, `품목 분류`, `대표 품목`, `진행 순서`, `문의 안내`, `기성 박스`, `맞춤 제작`, `길안내`처럼 페이지 성격에 맞는 짧은 한국어 라벨로 통일했습니다.
- 기대 효과
  템플릿 느낌이 줄고, 한국어 B2B 사이트 톤이 더 정돈되게 보이도록 맞췄습니다.
- 관련 파일
  `index.html`
  `products.html`
  `contact.html`

## 14. [대기] 현장 사진 신뢰도 보강과 이미지 재사용 축소

- 현재 상태
  홈 히어로, 현장 사진, 문의 페이지에서 같은 매장 사진이 반복 사용됩니다.
- 문제
  사진 증빙 섹션이 `실물 참고`보다 `같은 이미지 재활용`처럼 보일 수 있습니다.
- 영향
  현장성, 운영 신뢰도 전달이 약해질 수 있습니다.
- 권장 방향
  반복 이미지를 줄이고 사진별 역할을 더 분명히 나누는 방식 검토.
- 관련 파일
  `index.html`
  `contact.html`

## 15. [대기] 이미지 로딩 최적화와 레이아웃 안정성 보강

- 현재 상태
  주요 이미지에 `loading`, `width`, `height` 지정이 거의 없습니다.
- 문제
  정적 사이트에서 비교적 쉽게 줄일 수 있는 초기 로딩 비용과 레이아웃 흔들림 여지가 남아 있습니다.
- 영향
  체감 성능과 CLS 관점에서 손해가 있을 수 있습니다.
- 권장 방향
  below-the-fold 이미지 lazy-load, 치수 지정, 필요한 경우 우선순위 정리 검토.
- 관련 파일
  `index.html`
  `contact.html`
  `products.html`
  `product-*.html`

## 16. [대기] 결제·거래 안내 문구 정합성 통일

- 현재 상태
  홈과 `거래안내`에서 결제 방식 설명 강도와 정보 범위가 다릅니다.
- 문제
  사용자가 어디를 먼저 읽느냐에 따라 결제 관련 인상이 달라질 수 있습니다.
- 영향
  운영 기준이 분산돼 보일 수 있습니다.
- 권장 방향
  홈은 요약, `거래안내`는 기준 문서 역할로 문구 수준을 다시 맞추는 방식 검토.
- 관련 파일
  `index.html`
  `shipping.html`

## 17. [완료] 모바일 메뉴 접근성 보강

- 반영 결과
  모바일 메뉴에 `Esc` 닫기, 바깥 클릭 닫기, 스크롤 잠금, 현재 메뉴 `aria-current` 표시를 공통 스크립트로 추가했습니다.
- 개선 내용
  메뉴 열림 상태에 맞춰 `hidden`, `aria-hidden`, `aria-expanded`, `aria-label`을 같이 동기화하고, 메뉴 링크 클릭이나 데스크톱 폭 전환 시에도 자동으로 닫히게 맞췄습니다.
- 기대 효과
  모바일 탐색 중 현재 위치와 메뉴 상태가 더 분명해지고, 키보드·보조기기 사용자 기준의 기본 접근성이 보강됩니다.
- 관련 파일
  `assets/site.js`
  `assets/style.css`

## 18. [대기] 문서형 페이지 마크업 정리와 인라인 스타일 제거

- 현재 상태
  `shipping.html`, `tax.html`은 문서 구간이 압축된 단일 줄 마크업에 가깝고 인라인 패딩도 포함되어 있습니다.
- 문제
  추후 수정 시 가독성이 낮고 실수 가능성이 큽니다.
- 영향
  유지보수 속도와 안정성이 떨어집니다.
- 권장 방향
  구조는 유지하되 줄바꿈, 섹션 분리, 공통 클래스화 수준으로 정리 검토.
- 관련 파일
  `shipping.html`
  `tax.html`
  `assets/style.css`

## 19. [대기] CTA 문구와 버튼 톤 일관성 통일

- 현재 상태
  `맞춤견적 요청`, `견적요청`, `전화문의`, `전화 문의`처럼 문구가 섞여 있습니다.
- 문제
  같은 행동이 페이지마다 다른 언어로 보입니다.
- 영향
  브랜드 톤과 UI 일관성이 약해집니다.
- 권장 방향
  사이트 전역 CTA 문구 기준을 정하고 동일한 행동에는 같은 표현을 쓰는 방식 검토.
- 관련 파일
  `index.html`
  `products.html`
  `quote.html`
  `contact.html`
  `shipping.html`
  `tax.html`
  `product-*.html`

## 20. [대기] 구조화 데이터 확장 검토

- 현재 상태
  홈에는 최소 수준의 `LocalBusiness` 구조화 데이터만 들어 있습니다.
- 문제
  페이지별 성격이 달라졌는데 구조화 데이터는 아직 확장되지 않았습니다.
- 영향
  검색엔진에 주는 문맥 정보가 제한적입니다.
- 권장 방향
  우선순위가 높은 페이지 중심으로 구조화 데이터 보강 가능성 검토.
- 관련 파일
  `index.html`
  `products.html`
  `contact.html`
  `shipping.html`
## 검토 메모

- 정적 점검 기준으로 깨진 상대 링크와 이미지 경로는 발견하지 못했습니다.
- `robots.txt` 와 `assets/config.js`의 기본 연결 상태는 문제 없어 보였습니다.
